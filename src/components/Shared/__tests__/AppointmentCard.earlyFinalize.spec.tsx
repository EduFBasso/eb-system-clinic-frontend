// Tests for early-finalize closedLabel pill removed: feature was removed (server-side status replaces client-side override system)
import { describe, it } from 'vitest';
describe.skip('AppointmentCard early finalize pill with original_end_at', () => {
    it.skip('feature removed', () => {});
});
