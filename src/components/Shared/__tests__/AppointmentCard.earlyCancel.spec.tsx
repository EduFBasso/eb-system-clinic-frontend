// Tests for early-cancel closedLabel pill removed: feature was removed (server-side status replaces client-side override system)
import { describe, it } from 'vitest';
describe.skip('AppointmentCard early cancel pill', () => {
    it.skip('feature removed', () => {});
});
